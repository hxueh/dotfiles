# Use the macOS GUI prompt that can save to Keychain
pinentry-program /opt/homebrew/bin/pinentry-mac

# Optional: keep the unlocked key cached longer
default-cache-ttl 31536000     # 365 days
max-cache-ttl     31536000
